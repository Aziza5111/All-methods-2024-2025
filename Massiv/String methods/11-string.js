let str = "muz";
let firstLetter = str.charAt(0);
let lastLetter = str.charAt(str.length - 1);

let middleLetter = str.slice(1, str.length - 1);

console.log(lastLetter + middleLetter + firstLetter);
